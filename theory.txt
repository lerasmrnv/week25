1. Вообще не обязательно, желаельно иметь только установленнные node.js и npm. Можно установить реакт через терминал.
2. Приложения везде работают, можно работать с большим объемом данных, лучше производительность и скорость работы, доступ к фреймворкам.
Из минусов, необходимо постоянная работа интернета, тяжело работать с сео, не работает, если отключен js.
3.Для начала необходимо инициализировать реакт приложение.  
npm install -g create-react-app
create-react-app  *Здесь название проекта*
npm start - запуститься приложение
 npm build - для подготовки приложения к продакшену
4. По сути реакт это библиотека, но это также и фреймворк)
еще есть angular, vue.js, Ember.js.
5. СНачала устанавливаем через терминал библиотеку npm install *библиотека*,
далее пишем 
import { *компонент* } from '*библиотека*';
6. React Photo Gallery, React Dates, React Slick
7. src/App.js
8. Эта папка нужна для подготовки проекта к продакшену и запуску на хостинг. 
Используют команду npm build